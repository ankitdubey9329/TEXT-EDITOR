# google-doc



use `npm start` for running the client
use `npm run servStart` to run the server

This is a text editor wherein you can write data and format it as you wish, your document can be open on *2 screens* and both users can edit it simultaneously
The print page will not display any of the elements of the editor except for plain text.

*GO AHEAD AND MAKE YOUR NOTES HERE!!*
